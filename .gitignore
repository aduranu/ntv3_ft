# build / cache
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# checkpoints + per-run logs (regenerated each run)
best.pkl
*.pkl
_test_run_*.out
*.log

# run artifacts: SLURM logs, per-epoch metrics, generated plots, scratch state
*.out
sweep_logs/
metrics_*.csv
curves*.csv
*.png
sweep_jobs.txt
sweep_ext_jobs.txt
.last_jobs
.smoke_job
.verify_job

# dataset + raw downloads (multi-GB; rebuild with `python data.py --build`)
data/
jores21-raw/

# editor / OS
.vscode/
.idea/
.DS_Store
